<% option_groups(question).each_with_index do |options, index| %>
  <ul class="options multi-checkbox-options <%= "hidden" if index > 0 %>">
    <% if options.length > 4 %>
      <% sub_groups = options.each_slice(4).to_a %>
      <% sub_groups.each do |sub_group| %>
        <div class="column c-<%=sub_groups.length%>">
          <% sub_group.each do |option| %>
            <%= render partial: "questions/checkbox_option", locals: {option: option} %>
          <% end %>
        </div>
      <% end %>
    <% else %>
      <% options.each do |option| %>
        <%= render partial: "questions/checkbox_option", locals: {option: option} %>
      <% end %>
    <% end %>
  </ul>
<% end %>
