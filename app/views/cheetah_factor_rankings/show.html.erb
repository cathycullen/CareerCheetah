<% content_for :previous_url do %>
  <% if @previous_selection %>
    <%= program_phase_section_cheetah_factor_ranking_path(@program, @section.phase, @section, @previous_selection) %>
  <% else %>
    <%= program_phase_section_path(@section, @section.phase, @section) %>
  <% end %>
<% end %>
<% content_for :next_url do %>
  <% if @next_selection %>
    <%= program_phase_section_cheetah_factor_ranking_path(@program, @section.phase, @section, @next_selection) %>
  <% else %>
    <%= program_phase_section_section_step_path(@section, @section.phase, @section, @section.section_steps.find_by(:type => "ConclusionStep")) %>
  <% end %>
<% end %>

<div class="current-step question">
  <div class="question">
    <p class="prompt"><%= @cheetah_factor.rating_prompt %></p>
    <div class="option-group">
      <ul class="rating-options">
        <% rating_options.each do |option| %>
          <li class="option rating-option">
            <%= label_tag nil, :class => "response-option" do %>
              <span class="value-description"><%= option[:value_description]%></span><br />
              <%= radio_button_tag "cheetah-factor-rankings[]",
                                   option[:value],
                                   @cheetah_factor_ranking.original_rating == option[:value],
                                   {"data-cheetah-factor-ranking-id" => "#{@cheetah_factor_ranking.id}"} %><br />
              <%= option[:description] %>
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
